import { RouteObject } from 'react-router-dom';

import ModuleRoot from './components';

const routes: RouteObject[] = [
  {
    path: '/',
    element: <ModuleRoot />,
  },
];

export default routes;
